# PR #28: consolidated baseline and isolated Preview

Evidence date: September 8, 2026 (UTC). This delivery updates the existing
`fix/webmcp-type-contract` proposal and preserves `HANDOFF-2026-09-06.md`.

## Validated baseline

- Node 24.14.0 / npm 11.6.1: clean installation succeeded; direct dependencies retained.
- Consolidated lint: no errors or warnings. Tests: 447 total, 445 passed, 0 failed, 2 skipped.
- The two explicit skips are live official MCP and live Dexalot Testnet checks, which require separate external acceptance.
- Next.js build and TypeScript completed. Local evidence is in [the dated evidence directory](audits/preview-pr28-2026-09-08/quality.txt).
- Shared WebMCP registration, cleanup, fresh session handling and HTTP errors retain the complete local implementation and tests. Direct Solana faucet registration is removed; malformed or unconfirmed funding requests fail closed.
- Bazaar, credentials and temporary working files are excluded from source/build/package inputs.

## Preview isolation

Project: `agente-asistente` / `prj_UQnTOdi1AWU6soTr04ACsqNo7YDu`.
QA database: `carmelita-preview-pr28`, Neon project `fancy-art-64243805`,
Vercel resource `store_XLsI5Pw9YPkHBbfA`. Created empty on the existing free plan;
no production data was copied and Neon Auth was not enabled.

The new integration is connected only to Preview under unused `CARMELITA_QA_*`
names. Runtime and migration variables are separately scoped to Preview **and**
`fix/webmcp-type-contract`: `CARMELITA_PREVIEW_DATABASE_URL` and
`CARMELITA_PREVIEW_DATABASE_URL_UNPOOLED`, plus the explicit isolation
host/origin/deployment controls. Preview and its local acceptance/migration
runners use only this dedicated pair, with no Marketplace/legacy fallback.
`UNBLCK_API_ENABLED=false` is branch-specific.

The first deployment (`bfc837b`) correctly rejected a conflicting automatic
`DATABASE_URL_UNPOOLED` value with HTTP 503 before application database access.
Branch overrides of legacy names alone were insufficient. Dedicated names remove
that ambiguity without changing the existing production integration. The first
console smoke also found a Vercel CLI flag incompatibility; the runner now uses
the supported transport flags. These failures are not acceptance passes.

Database fingerprint: `bfd2efdf0f2fec5299c643ab9ef012c2648d8baa2d83297ff3f924b6bd123299`.
The runtime exposes only this hash to acceptance scripts. The canonical QA host
allowlist rejects any different resource, including production. Production's
hostname was not retrievable through its sensitive Vercel variable; identity
separation is established by the newly created resource and exact QA allowlist.

The independent guarded migration succeeded, followed by an idempotent repeat:
0 public tables before, 38 afterwards, 20 journal entries. Migration 0019 adds
the previously missing `agent_x402_events` table. Historical SQL remains intact.
DDL and journal updates now share a transaction with concurrency protection.
Historical LF/CRLF hashes are accepted only for equivalent line endings; new
entries use canonical LF, so Windows and Vercel recognize the same migrations.
Schema snapshots currently end at 0016; reconcile manual later migrations before
a future automatic schema generation. The historical journal's dialect label
does not affect the PostgreSQL runner.

Before push, Vercel's project-level build override was absent. Versioned
`vercel.json` now builds with `npm run build`; migrations run only through the
explicit independent Preview step. The production domain still resolves to
`dpl_tYAhcpzd9uxqAYPU6869jLzt7dbG` (Ready); the latest failed production target
is a different deployment and must not be confused with live traffic.

## Acceptance and remaining evidence

The runners require explicit URL/deployment/commit and verify remote environment,
commit and database fingerprint before authenticated writes. Bootstrap requires
an explicit flag and matching Origin. Acceptance runners cannot execute financial
actions. Gateway execution remains disabled; application payments require visible
approval and signing and were not exercised. Gateway fixtures are synthetic and
never count as Privy login proof.
Cleanup uses exact plan/token IDs, run-specific request IDs and scoped pseudonyms;
the report gives deleted counts and preserves shared or historical records.
See [acceptance testing](acceptance-testing.md) for the current interfaces.

## Remote evidence

Validated application commit: `4c9d642e10194319e06c1bc27535938e44cb2848`.
Deployment: `dpl_6S1bqWjTAB2AcUrVXjXyd6e4fhsp`, Ready, build command `npm run build`.

- [Immutable validated Preview](https://agente-asistente-ewb24zqe8-cabscryptocontacto-6028s-projects.vercel.app).
- [Branch Preview](https://agente-asistente-git-f-22838e-cabscryptocontacto-6028s-projects.vercel.app).
- [GitHub quality run](https://github.com/CaBsCrypto/carmelita/actions/runs/34186101871): passed in 2m22s, including clean installation, lint, tests and build.

At 04:14–04:17 UTC, the [public checks](audits/preview-pr28-2026-09-08/public-preview.json)
passed 7/7. [Runtime health](audits/preview-pr28-2026-09-08/health.json) verified
Preview environment, exact commit and QA database fingerprint. Health alone is
not a login, persistence or payment acceptance test.

The [Gateway run](audits/preview-pr28-2026-09-08/gateway.json) passed 26 checks,
with 0 failures and 1 explicit pending human acceptance group. It verified
REST/MCP scopes, idempotent plan replay, changed-input conflict, account-isolated
plan access, receipt access control, disabled execution and revoked-token rejection.
It deleted 3 plans, 3 tokens, 6 actor/token-specific rate buckets and 19 exact
request audit events. The two actors were synthetic fixtures, not Privy users.

Browser observation on the internal Vercel-authorized browser verified home →
integration lab → developer portal navigation. After navigation and reload,
exactly `search_agent_offers` and `prepare_commerce_intent` were registered;
there was no faucet tool. A read-only no-match query returned an empty offer
list with Postgres persistence. The earlier HTTP failure was exposed as
`isError: true`. Lifecycle teardown and missing-browser-support behavior are
covered locally; no separate unsupported-browser live acceptance is claimed.

[Final QA counts](audits/preview-pr28-2026-09-08/database-final.json) show zero
users, wallets, Gateway plans/tokens/rate buckets, x402 payments, Stellar actions,
faucet claims and commerce intents. One anonymous public-capability audit event
remains as diagnostic evidence; no broad cleanup was performed.

Production was rechecked at closure and still resolves to the same original
Ready deployment. Bazaar has no source changes.

Two dedicated Privy identities, allowed login origin, actual login/session
recovery, wallet stability, chat/memory persistence and isolation between real
sessions remain pending. The two test email identities and their visible login
steps have been requested from the operator. No test accounts have been invented
or replaced with real user sessions. Historical landing/integration status text
also needs alignment with the current evidence before a broader product acceptance.
No payments, funding, reservations or Telegram calls are part of this delivery.

Graphify update was attempted after code changes and failed because its launcher
references an unavailable Python 3.12 executable. No updated graph is claimed.

Travala remains pending after the previously observed HTTP 401. Notion/OAuth,
Stellar payment acceptance and Bazaar consumption remain separate later gates.
